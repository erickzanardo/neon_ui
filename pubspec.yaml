name: neon_ui
description: A Flutter UI library inspired by Neon Vibes
version: 0.0.1
homepage: https://github.com/erickzanardo/neon_ui

environment:
  sdk: ^3.9.0
  flutter: >=3.35.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.2

dev_dependencies:
  build_runner: ^2.10.4
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.0.0
